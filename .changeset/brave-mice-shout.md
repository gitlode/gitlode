---
"gitlode": minor
---

[Changed] gitlode now outputs commit text as a single `message` field in JSONL records instead of separate `subject` and `body`; use `@gitlode/plugin-conventional-commits` if you need split commit-message fields.
