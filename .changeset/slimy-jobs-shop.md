---
"gitlode": minor
---

[Changed] Updated the plugin interface so `skip`/`fatal` results no longer include message fields, and plugins must emit their own warnings/errors via runtime diagnostics; plugin packages need compatible updates when upgrading gitlode.
