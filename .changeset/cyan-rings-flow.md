---
"gitlode": patch
---

[Changed] Renamed file-level profiling spans to reflect their owning components: `git.file_changes` is now `gitlode.file_change_expansion`, and `git.diff` is now `line_diff.compute`.
