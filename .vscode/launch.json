{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "gitlode: full extract",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/packages/gitlode/src/index.ts",
      "args": [
        "${input:repo}",
        "--ref",
        "${input:ref}",
        "--output-dir",
        "${workspaceFolder}/packages/gitlode/out",
        "--config",
        "${input:config}"
      ],
      "preLaunchTask": "npm: build",
      "outFiles": ["${workspaceFolder}/packages/gitlode/dist/**/*.js"],
      "sourceMaps": true,
      "skipFiles": ["<node_internals>/**", "${workspaceFolder}/node_modules/**"],
      "console": "integratedTerminal",
      "outputCapture": "std",
      "env": {
        "GITLODE_EXPERIMENTAL_COMMIT_TRAVERSAL": "phase-certified-timestamp"
        // "NODE_ENV": "development",
        // "DEBUG": "isomorphic-git:*" // Enable debug logging for isomorphic-git
      }
    },

    {
      "name": "gitlode: continuous extract",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/src/index.ts",
      "args": [
        "${input:repo}",
        "--ref",
        "${input:ref}",
        "--output-dir",
        "${workspaceFolder}/packages/gitlode/out",
        "--config",
        "${input:config}",
        "--state",
        "${workspaceFolder}/out/state.json"
      ],
      "preLaunchTask": "npm: build",
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "sourceMaps": true,
      "skipFiles": ["<node_internals>/**", "${workspaceFolder}/node_modules/**"],
      "env": {
        "GITLODE_EXPERIMENTAL_COMMIT_TRAVERSAL": "phase-certified-timestamp"
        // "NODE_ENV": "development",
        // "DEBUG": "isomorphic-git:*" // Enable debug logging for isomorphic-git
      }
    }
  ],
  "inputs": [
    {
      "id": "repo",
      "type": "promptString",
      "description": "Path to the Git repository",
      "default": "${workspaceFolder}"
    },
    {
      "id": "ref",
      "type": "promptString",
      "description": "Git ref/branch",
      "default": "main"
    },
    {
      "id": "config",
      "type": "promptString",
      "description": "Path to the config file",
      "default": "${workspaceFolder}/packages/gitlode/out/config.json"
    }
  ]
}
